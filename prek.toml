[[repos]]
repo = "local"

hooks = [
  { id = "pre-commit", name = "pre-commit", entry = "npm run precommit", language = "system", stages = ["pre-commit"] },
  { id = "pre-push", name = "pre-push", entry = "npm run prepush", language = "system", stages = ["pre-push"] }
]
