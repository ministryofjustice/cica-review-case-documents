name: Deploy to environment

on:
    workflow_dispatch:
        inputs:
            environment:
                description: Environment to deploy to
                required: true
                type: choice
                options:
                    - dev
                    - uat
                    # - prod   # Uncomment when ready to support production
                default: dev
            ref:
                description: Git ref (branch, tag, or SHA) to deploy
                required: true
                default: main
                type: string
            skip_security_scan:
                description: Skip security vulnerability scan (emergency deployments only)
                required: false
                type: boolean
                default: false

permissions:
    contents: read
    id-token: write # Required for OIDC authentication with AWS

jobs:
    build-and-scan:
        name: Build and scan image for ${{ inputs.environment }}
        environment: ${{ inputs.environment }}
        env:
            SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        runs-on: ubuntu-latest
        permissions:
            id-token: write
            contents: read
            security-events: write
        outputs:
            image-tag: ${{ github.sha }}
            registry: ${{ steps.login-ecr.outputs.registry }}
            repository: ${{ vars.ECR_REPOSITORY }}

        steps:
            - name: Checkout code
              uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 #v6.0.3
              with:
                  ref: ${{ inputs.ref || github.sha }}

            - name: Set up Node.js
              uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e #v6.4.0
              with:
                  node-version: "24.15.0"

            - name: Show deployment info
              run: |
                  echo "Building and scanning for environment ${{ inputs.environment }}"
                  echo "Workflow triggered from branch: ${{ github.ref_name }}"
                  echo "Workflow triggered by: ${{ github.event_name }}"
                  echo "Deploying ref/branch: ${{ inputs.ref || github.ref_name }}"
                  echo "Commit SHA being deployed: ${{ github.sha }}"
                  echo "Skip security scan: ${{ inputs.skip_security_scan || 'false' }}"

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@e7f100cf4c008499ea8adda475de1042d6975c7b # v6.2.0
              with:
                  role-to-assume: ${{ secrets.ECR_ROLE_TO_ASSUME }}
                  aws-region: ${{ vars.ECR_REGION }}
                  audience: sts.amazonaws.com

            - name: Login to Amazon ECR
              uses: aws-actions/amazon-ecr-login@fa648b43de3d4d023bcb3f89ed6940096949c419 #v2.1.5

              id: login-ecr

            - name: Install Snyk CLI
              if: ${{ inputs.skip_security_scan != true }}
              run: npm install -g snyk

            - name: Install dependencies
              run: npm ci

            - name: Scan application (SARIF + policy evaluation)
              id: scan_application
              if: ${{ inputs.skip_security_scan != true }}
              run: |
                  set -euo pipefail

                  echo "=== APPLICATION SCAN START ==="

                  # Allow command to fail without killing step
                  set +e
                  # Capture stdout (JSON) and stderr (diagnostics) separately so downstream steps
                  # can parse clean JSON via jq and access error context from the log file
                  snyk test \
                      --policy-path=.snyk \
                      --all-projects \
                      --severity-threshold=high \
                      --json > snyk-test-results.json 2> snyk-test-results.log

                  EXIT_CODE=$?
                  set -e

                  echo "Snyk raw exit code: $EXIT_CODE"

                  # Normalize JSON output so downstream cat/jq steps are deterministic.
                  # Keep EXIT_CODE-based gating as the source of pass/fail truth.
                  if [ ! -s snyk-test-results.json ] || ! jq -e . snyk-test-results.json >/dev/null 2>&1; then
                      echo "::warning::snyk-test-results.json missing or invalid; writing fallback JSON"
                      printf '{"ok":false,"error":"snyk test --json produced no valid JSON","exitCode":%s,"vulnerabilities":[],"applications":[]}\n' "$EXIT_CODE" > snyk-test-results.json
                  fi

                  # Convert to SARIF (always succeeds)
                  # Append stderr from SARIF conversion to the same log for complete diagnostics
                  snyk test \
                      --policy-path=.snyk \
                      --all-projects \
                      --severity-threshold=high \
                      --sarif > snyk-test-results.sarif 2>> snyk-test-results.log || true

                  # Ensure SARIF exists
                  if [ ! -s snyk-test-results.sarif ]; then
                      echo '{"version":"2.1.0","runs":[]}' > snyk-test-results.sarif
                  fi

                  echo "=== APPLICATION VULN SUMMARY ==="

                  if [ -s snyk-test-results.json ]; then
                      if ! jq '
                        def all_vulns:
                            [
                                (.vulnerabilities[]?),
                                (.applications[]?.vulnerabilities[]?)
                            ];
                        {
                            total: (all_vulns | length),
                            high: (all_vulns | map(select(.severity=="high")) | length),
                            critical: (all_vulns | map(select(.severity=="critical")) | length),
                            fixable: (all_vulns | map(select(.isUpgradable==true or .isPatchable==true)) | length)
                        }' snyk-test-results.json; then
                          echo "::warning::Unable to parse snyk-test-results.json; scan exit code was $EXIT_CODE"
                      fi
                  else
                      echo "::warning::No snyk-test-results.json produced; scan exit code was $EXIT_CODE"
                  fi

                  echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT

            - name: Upload dependency SARIF to GitHub
              if: ${{ inputs.skip_security_scan != true  && hashFiles('snyk-test-results.sarif') != '' }}
              uses: github/codeql-action/upload-sarif@eda5730a8bfb740e03a28087a958444c646e5842 #v4.36.0
              with:
                  sarif_file: snyk-test-results.sarif

            - name: Debug SARIF output (application scan)
              if: ${{ inputs.skip_security_scan != true }}
              run: |
                  echo "=== STDERR ==="
                  cat snyk-test-results.log || true
                  echo "=== SARIF (first 20 lines) ==="
                  head -n 20 snyk-test-results.sarif || true

            - name: Dump full application Snyk JSON
              if: ${{ inputs.skip_security_scan != true }}
              run: |
                  echo "=== FULL APPLICATION SNYK JSON OUTPUT ==="
                  cat snyk-test-results.json | jq . || cat snyk-test-results.json

            - name: Build Docker image
              env:
                  REGISTRY: ${{ steps.login-ecr.outputs.registry }}
                  REPOSITORY: ${{ vars.ECR_REPOSITORY }}
              run: |
                  echo "Building Docker image for $REGISTRY/$REPOSITORY"
                  docker build --build-arg NODE_ENV=production -t $REGISTRY/$REPOSITORY:${{ github.sha }} .

            - name: Security scan bypassed warning
              if: ${{ inputs.skip_security_scan == true}}
              run: |
                  echo "::warning::⚠️ SECURITY SCAN BYPASSED - This is an emergency deployment without vulnerability scanning"

            - name: Scan container (SARIF + policy evaluation)
              id: scan_container
              if: ${{ inputs.skip_security_scan != true }}
              env:
                  REGISTRY: ${{ steps.login-ecr.outputs.registry }}
                  REPOSITORY: ${{ vars.ECR_REPOSITORY }}
              run: |
                  set -euo pipefail

                  IMAGE="$REGISTRY/$REPOSITORY:${{ github.sha }}"

                  echo "=== CONTAINER SCAN START ==="
                  echo "Scanning image: $IMAGE"

                  set +e
                  # Capture stdout (JSON) and stderr (diagnostics) separately so downstream steps
                  # can parse clean JSON via jq and access error context from the log file
                  snyk container test "$IMAGE" \
                      --policy-path=.snyk \
                      --file=Dockerfile \
                      --severity-threshold=high \
                      --json > snyk-container-test-results.json 2> snyk-container-test-results.log

                  EXIT_CODE=$?
                  set -e

                  echo "Snyk raw exit code: $EXIT_CODE"

                  # Normalize JSON output so downstream cat/jq steps are deterministic.
                  # Keep EXIT_CODE-based gating as the source of pass/fail truth.
                  if [ ! -s snyk-container-test-results.json ] || ! jq -e . snyk-container-test-results.json >/dev/null 2>&1; then
                      echo "::warning::snyk-container-test-results.json missing or invalid; writing fallback JSON"
                      printf '{"ok":false,"error":"snyk container test --json produced no valid JSON","exitCode":%s,"vulnerabilities":[],"applications":[]}\n' "$EXIT_CODE" > snyk-container-test-results.json
                  fi

                  # Append stderr from SARIF conversion to the same log for complete diagnostics
                  snyk container test "$IMAGE" \
                      --policy-path=.snyk \
                      --file=Dockerfile \
                      --severity-threshold=high \
                      --sarif > snyk-container-test-results.sarif 2>> snyk-container-test-results.log || true

                  if [ ! -s snyk-container-test-results.sarif ]; then
                      echo '{"version":"2.1.0","runs":[]}' > snyk-container-test-results.sarif
                  fi

                  echo "=== CONTAINER VULN SUMMARY ==="

                  if [ -s snyk-container-test-results.json ]; then
                      if ! jq '
                        def all_vulns:
                            [
                                (.vulnerabilities[]?),
                                (.applications[]?.vulnerabilities[]?)
                            ];
                        {
                            total: (all_vulns | length),
                            high: (all_vulns | map(select(.severity=="high")) | length),
                            critical: (all_vulns | map(select(.severity=="critical")) | length),
                            fixable: (all_vulns | map(select(.isUpgradable==true or .isPatchable==true)) | length)
                        }' snyk-container-test-results.json; then
                          echo "::warning::Unable to parse snyk-container-test-results.json; scan exit code was $EXIT_CODE"
                      fi
                  else
                      echo "::warning::No snyk-container-test-results.json produced; scan exit code was $EXIT_CODE"
                  fi

                  echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT

            - name: Dump full container Snyk JSON
              if: ${{ inputs.skip_security_scan != true }}
              run: |
                  echo "=== FULL CONTAINER SNYK JSON OUTPUT ==="
                  cat snyk-container-test-results.json | jq . || cat snyk-container-test-results.json

            - name: Upload container SARIF to GitHub
              if: ${{ inputs.skip_security_scan != true  && hashFiles('snyk-container-test-results.sarif') != '' }}
              uses: github/codeql-action/upload-sarif@eda5730a8bfb740e03a28087a958444c646e5842 #v4.36.0
              with:
                  sarif_file: snyk-container-test-results.sarif

            - name: Debug SARIF output (container scan)
              if: ${{ inputs.skip_security_scan != true }}
              run: |
                  echo "=== STDERR ==="
                  cat snyk-container-test-results.log || true
                  echo "=== SARIF (first 20 lines) ==="
                  head -n 20 snyk-container-test-results.sarif || true

            - name: Explain application scan result
              if: ${{ inputs.skip_security_scan != true }}
              run: |
                  echo "=== WHY DID THE APPLICATION SCAN NOT FAIL? ==="

                  if [ ! -s snyk-test-results.json ]; then
                  echo "⚠️ No Snyk JSON output found!"
                  else
                    VULNS=$(jq -r '
                    (
                    .vulnerabilities[]?,
                    .applications[]?.vulnerabilities[]?
                    )
                    | select(.severity=="high" or .severity=="critical")
                    | "- \(.id) | severity=\(.severity) | fixable=\((.isUpgradable == true) or (.isPatchable == true))"
                    ' snyk-test-results.json)

                    if [ -z "$VULNS" ]; then
                        echo "✅ No high/critical application vulnerabilities found."
                    else
                        echo "$VULNS"
                    fi
                  fi

            - name: Explain container scan result
              if: ${{ inputs.skip_security_scan != true }}
              run: |
                  echo "=== WHY DID THE CONTAINER SCAN NOT FAIL? ==="

                  if [ ! -s snyk-container-test-results.json ]; then
                  echo "⚠️ No Snyk JSON output found!"
                  else
                    VULNS=$(jq -r '
                    (
                    .vulnerabilities[]?,
                    .applications[]?.vulnerabilities[]?
                    )
                    | select(.severity=="high" or .severity=="critical")
                    | "- \(.id) | severity=\(.severity) | fixable=\((.isUpgradable == true) or (.isPatchable == true))"
                    ' snyk-container-test-results.json)

                    if [ -z "$VULNS" ]; then
                        echo "✅ No high/critical container vulnerabilities found."
                    else
                        echo "$VULNS"
                    fi
                  fi

            - name: ⚠️ Report policy-suppressed vulnerabilities
              if: ${{ inputs.skip_security_scan != true }}
              run: |
                  echo "=== SNYK POLICY FILE (.snyk) - IGNORED VULNERABILITIES ==="
                  echo ""
                  if [ -f .snyk ]; then
                      cat .snyk
                      echo ""
                      echo "=== POLICY SUMMARY ==="
                      ACTIVE_IGNORES=$(awk '
                        BEGIN { in_ignore = 0; count = 0 }
                        /^ignore:[[:space:]]*$/ { in_ignore = 1; next }
                        in_ignore && /^[^[:space:]]/ { in_ignore = 0 }
                        in_ignore && /^[[:space:]]{2}[A-Za-z0-9._-]+:[[:space:]]*$/ { count++ }
                        END { print count }
                      ' .snyk)

                      echo "Active ignore entries detected: $ACTIVE_IGNORES"

                      if [ "$ACTIVE_IGNORES" -gt 0 ]; then
                          echo "::warning::Policy file detected and applied via --policy-path=.snyk"
                          echo "::warning::Active suppressions are present in .snyk"
                          echo "::warning::Review the .snyk file above to verify suppressions have clear reason and expiry"
                      else
                          echo "No active suppressions found in .snyk"
                      fi
                  fi

            - name: Fail on non-fixable container vulnerabilities
              if: ${{ inputs.skip_security_scan != true }}
              run: |
                  if [ ! -s snyk-container-test-results.json ]; then
                      echo "::warning::No snyk-container-test-results.json found; skipping non-fixable check"
                      exit 0
                  fi

                  NON_FIXABLE=$(jq '
                    [
                    (.vulnerabilities[]?),
                    (.applications[]?.vulnerabilities[]?)
                    ]
                    | map(select(
                        (.severity=="high" or .severity=="critical")
                        and (.isUpgradable!=true and .isPatchable!=true)
                    ))
                    | length
                    ' snyk-container-test-results.json)

                  if [ "$NON_FIXABLE" -gt 0 ]; then
                      echo "❌ Failing build: $NON_FIXABLE high/critical container vulnerabilities found that are NOT fixable"
                      echo "These must be acknowledged via skip_security_scan for emergency deployments only."
                      exit 1
                  fi

            - name: Fail workflow on application vulnerabilities
              if: ${{ inputs.skip_security_scan != true && steps.scan_application.outputs.exit_code != '0' }}
              run: |
                  EXIT_CODE="${{ steps.scan_application.outputs.exit_code }}"

                  if [ "$EXIT_CODE" = "1" ]; then
                      echo "❌ High/critical application vulnerabilities found"
                      echo "These must be acknowledged via skip_security_scan for emergency deployments only."
                  elif [ "$EXIT_CODE" = "2" ]; then
                      echo "❌ Snyk application scan encountered an error (exit code 2)"
                      echo "This could indicate authentication failure, network issues, or misconfiguration."
                      echo "Please check the scan output above for details."
                  else
                      echo "❌ Snyk application scan failed with unexpected exit code: $EXIT_CODE"
                  fi

                  exit 1

            - name: Fail workflow on container scan error
              if: ${{ inputs.skip_security_scan != true && steps.scan_container.outputs.exit_code == '2' }}
              run: |
                  echo "❌ Snyk container scan encountered an error (exit code 2)"
                  echo "This could indicate authentication failure, network issues, or misconfiguration."
                  echo "Please check the scan output above for details."
                  exit 1

            - name: Fail on fixable container vulnerabilities (jq evaluation)
              if: ${{ inputs.skip_security_scan != true }}
              run: |
                  set -euo pipefail

                  echo "=== EVALUATING CONTAINER VULNERABILITIES (FIXABLE ONLY) ==="

                  if [ ! -s snyk-container-test-results.json ]; then
                      echo "⚠️ No Snyk JSON output found!"
                      exit 1
                  fi

                  FIXABLE=$(jq '
                      [
                      (.vulnerabilities[]?),
                      (.applications[]?.vulnerabilities[]?)
                      ]
                      | map(select(
                          (.severity=="high" or .severity=="critical")
                          and (.isUpgradable==true or .isPatchable==true)
                      ))
                      | length
                  ' snyk-container-test-results.json)

                  echo "Fixable high/critical vulnerabilities: $FIXABLE"

                  if [ "$FIXABLE" -gt 0 ]; then
                      echo "❌ Failing build due to fixable high/critical vulnerabilities"

                      echo ""
                      echo "=== LIST OF FAILING VULNERABILITIES ==="

                      jq -r '
                      [
                          (.vulnerabilities[]?),
                          (.applications[]?.vulnerabilities[]?)
                      ]
                      | map(select(
                          (.severity=="high" or .severity=="critical")
                          and (.isUpgradable==true or .isPatchable==true)
                      ))
                      | .[]
                      | "- \(.id) | package=\(.packageName) | severity=\(.severity) | fixable=true"
                      ' snyk-container-test-results.json

                      exit 1
                  else
                      echo "✅ No fixable high/critical vulnerabilities found"
                  fi

            - name: Monitor container (informational scan)
              if: ${{ inputs.skip_security_scan != true }}
              env:
                  REGISTRY: ${{ steps.login-ecr.outputs.registry }}
                  REPOSITORY: ${{ vars.ECR_REPOSITORY }}
              run: |
                  echo "Sending results to Snyk for continuous monitoring..."
                  snyk container monitor $REGISTRY/$REPOSITORY:${{ github.sha }}

            - name: Push Docker image
              env:
                  REGISTRY: ${{ steps.login-ecr.outputs.registry }}
                  REPOSITORY: ${{ vars.ECR_REPOSITORY }}
              run: |
                  echo "Pushing Docker image to $REGISTRY/$REPOSITORY"
                  docker push $REGISTRY/$REPOSITORY:${{ github.sha }}

    deploy:
        needs: build-and-scan
        name: Deploy to environment
        environment: ${{ inputs.environment }}
        runs-on: ubuntu-latest
        permissions:
            id-token: write
            contents: read

        steps:
            - name: Checkout code
              uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 #v6.0.3
              with:
                  ref: ${{ inputs.ref || github.sha }}

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@e7f100cf4c008499ea8adda475de1042d6975c7b # v6.2.0
              with:
                  role-to-assume: ${{ secrets.ECR_ROLE_TO_ASSUME }}
                  aws-region: ${{ vars.ECR_REGION }}
                  audience: sts.amazonaws.com

            - name: Login to Amazon ECR
              uses: aws-actions/amazon-ecr-login@fa648b43de3d4d023bcb3f89ed6940096949c419 #v2.1.5
              id: login-ecr

            - name: Template Kubernetes manifests
              env:
                  NODE_ENV: production
                  IMAGE_TAG: ${{ needs.build-and-scan.outputs.image-tag }}
                  REGISTRY: ${{ needs.build-and-scan.outputs.registry }}
                  REPOSITORY: ${{ needs.build-and-scan.outputs.repository }}
                  NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}
                  DEPLOY_ENV: ${{ inputs.environment }}
              run: |
                  echo "Templating Kubernetes manifests for $DEPLOY_ENV"
                  export HOST_NAME="cica-review-case-documents-$DEPLOY_ENV.apps.live.cloud-platform.service.justice.gov.uk"
                  echo "HOST_NAME is $HOST_NAME"
                  mkdir -p deployments
                  cat deployments/templates/deployment.yml | envsubst > deployments/deployment.yml
                  cat deployments/templates/ingress.yml | envsubst > deployments/ingress.yml
                  cat deployments/templates/service.yml | envsubst > deployments/service.yml

            - name: Deploy to Kubernetes
              env:
                  KUBE_CERT: ${{ secrets.KUBE_CERT }}
                  KUBE_CLUSTER: ${{ secrets.KUBE_CLUSTER }}
                  KUBE_NAMESPACE: ${{ secrets.KUBE_NAMESPACE }}
                  KUBE_TOKEN: ${{ secrets.KUBE_TOKEN }}
              run: |
                  echo "Deploying to cluster $KUBE_CLUSTER namespace $KUBE_NAMESPACE"
                  echo "$KUBE_CERT" > ca.crt
                  kubectl config set-cluster $KUBE_CLUSTER --certificate-authority=./ca.crt --server=https://$KUBE_CLUSTER
                  kubectl config set-credentials deploy-user --token=$KUBE_TOKEN
                  kubectl config set-context $KUBE_CLUSTER --cluster=$KUBE_CLUSTER --user=deploy-user --namespace=$KUBE_NAMESPACE
                  kubectl config use-context $KUBE_CLUSTER
                  kubectl -n $KUBE_NAMESPACE apply -f deployments/

# Recommended next steps for prod:
# - Add 'prod' to the environment options above
# - Create a 'prod' environment in GitHub with required reviewers/approvals
# - Restrict prod deployments to main branch or releases
# - Add notification steps for successful prod deployments
